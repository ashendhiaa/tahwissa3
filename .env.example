# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.mjs"
# should be updated accordingly.

# Database
# PostgreSQL connection string - replace with your actual database URL
DATABASE_URL="postgresql://username:password@localhost:5432/tahwissa_db"

# Authentication
# Secret key for JWT token signing - generate a secure random string
JWT_SECRET="your-jwt-secret-key-here"

# Environment
NODE_ENV="development"

# Mapbox (for maps and geocoding)
# Get these from https://account.mapbox.com/
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN="pk.your_mapbox_access_token"
NEXT_PUBLIC_MAPBOX_USERNAME="your_mapbox_username"
NEXT_PUBLIC_MAPBOX_STYLE_ID="your_mapbox_style_id"

# Google Maps Places API (for hotels and restaurants)
# Get from https://console.cloud.google.com/
NEXT_PUBLIC_GOOGLE_API_KEY="your_google_api_key"

# OpenWeatherMap API (for weather data)
# Get from https://openweathermap.org/api
NEXT_PUBLIC_API_KEY="your_openweathermap_api_key"

# TripAdvisor API (for hotel/restaurant details)
# Get from https://developer-tripadvisor.com/
NEXT_PUBLIC_TRIPADVISOR_API_KEY="your_tripadvisor_api_key"

# ImageKit (for image processing)
# Get from https://imagekit.io/
NEXT_PUBLIC_IMAGE_KIT_PRIVATE_KEY="your_imagekit_private_key"
